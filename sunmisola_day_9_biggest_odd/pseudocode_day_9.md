FUNCTION biggest_odd(numbers)

    CREATE a list called odd_numbers

    FOR each number in numbers
        IF number is odd
            ADD number to odd_numbers
        END IF
    END FOR

    RETURN the largest number in odd_numbers

END FUNCTION